-
    2. Auto filling of the form
-
    # Create the stock round
    !record {model: stock.round, id: stock_routing_stock_round_test2}:
        name: Stock routing Test 2
-
    # Create the partner
    !record {model: res.partner, id: stock_routing_res_partner_test2}:
        name: Stock routing Test 2
        round_id: stock_routing_stock_round_test2
-
    # Call the "onchange_shipping_address" method for the sale order and check the returned value
    !python {model: sale.order}: |
        res = self.onchange_shipping_address(cr, uid, [], ref('stock_routing_res_partner_test2'), False, False)
        assert res['value']['round_id'] == ref('stock_routing_stock_round_test2'), 'Assertion "Automatic filling" FAILED'

